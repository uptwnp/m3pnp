export interface LeadFormData {
  name: string;
  phone: string;
  email: string;
  interest: string;
}

export interface PriceData {
  size: string;
  price: string;
}

export interface Amenity {
  icon: string;
  title: string;
  description: string;
}

export interface FAQ {
  q: string;
  a: string;
}